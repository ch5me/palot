---
"@ch5me/elf-desktop": minor
---

Surround the project-manager "elf" hero wordmark with a GPU curl-noise particle swarm (`SwarmScene` from `@ch5me/effects/particles`). Adds `three` + React-Three-Fiber to the desktop app and a new `ElfHero` component that layers the gradient wordmark over an ambient FBO particle field.
