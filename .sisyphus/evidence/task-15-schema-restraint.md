# Task 15 Schema Restraint <!-- oc:id=sec_aa -->

Detail view behavior:

- Shows transport, auth, health, ownership, tool count, and read/write summary
- Keeps full schema payloads out of the default detail surface
- Uses explicit note: schema details stay collapsed until a describe action requests one tool contract

Result: users can judge trust/setup state without dumping full tool schemas into first paint.