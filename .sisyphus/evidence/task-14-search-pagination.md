# Task 14 Search and Pagination <!-- oc:id=sec_aa -->

Catalog dialog behavior:

- Curated picks render in a dedicated `Curated picks` section before `Browse all`
- Search filters by server id, category, and tags
- Pagination uses `Prev` and `Next` controls over the long-tail list
- Empty state appears when the filtered list has no matches

Search proof target:
- searching `notion` keeps notion-like entries visible
- clearing search restores the full paginated browse list