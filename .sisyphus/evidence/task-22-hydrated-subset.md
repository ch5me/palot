# Task 22 Hydrated Subset <!-- oc:id=sec_aa -->

Plugins panel posture now distinguishes:

- `configured` vs `connected` via `posture`
- `active` vs `inactive` via `hydrated`
- top summary includes active MCP count

This gives a session/runtime-facing distinction between total configured servers and the currently active hydrated subset.