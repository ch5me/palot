# Task 5 notes <!-- oc:id=sec_aa -->

- Promoted Electron local presence to same attach/inferred semantics already used in browser/server mode by reusing `apps/server/src/services/opencode-active-sessions.ts` from main process.
- Binding lifecycle now treats `session.status { type: "idle" }` same as `session.idle` for attached-state restoration.
- `scripts/debug-sessions.ts` now overlays DB history with live active presence and `session.status()` truth, surfacing stale-recency and attached-but-unhydrated drift.