# Task 11 notes <!-- oc:id=sec_aa -->

- Canonical session truth now starts in `SessionEntry`, not only derived UI selectors.
- `Agent` shape carries presence, visibility, drift, and content-activity fields so later sidebar/tray work can share one model.
- Child divergence hooks started via `driftFlags`; deeper parent/child UX still pending later tasks.