## 2026-06-13

- Task 1 complete enough to proceed: canonical lane type model is in shared/preload/renderer/server seams, but workspace `bun run check-types` is still blocked by unrelated unused imports in `apps/desktop/src/renderer/components/nav-sidebar-tabs.tsx`.
- Task 2 focus: desktop manager already migrates and rewrites registry at init; server route layer still needs deterministic bootstrap parity for missing, empty, and malformed registries plus migration coverage.
- Server route file had to localize browser-lane migration helpers because importing `apps/desktop/src/shared/browser-lanes.ts` broke `apps/server` type generation due to `rootDir` constraints.
- Task 3 audit findings: `createEmptyBrowserLaneHealth()` is still generic and produces `cdp.state = unknown`, so direct-iframe callers need a surface-aware initializer/helper if we want truthful default health. Server route `/:laneId` action handler still returns `profile-locked` for `reset-profile` without ownership gating, and `GET /:laneId/health` can still fall through to `profile-locked`/`stopped` based on `profilePath` for attached Selkies lanes.
- Task 3b progress: manager now rejects profile reset for attached lanes, stop on attached lanes returns externally-managed health instead of managed-local stop/profile states, and server `/health` no longer falls through to `profile-locked` for attached lanes. Workspace typecheck still blocked only by unrelated unused imports in `apps/desktop/src/renderer/components/nav-sidebar-tabs.tsx`.
- Task 3c progress: browser panel copy/actions now distinguish target vs stream, managed vs attached, and gate restart/profile reset UI to managed-local lanes only. Still need explicit component proof or assertions for the new copy/action matrix.
- Task 4 progress: manager lifecycle now hard-fails `start`/`stop`/`restart`/`reset-profile` for attached lanes, leaving attached flows probe-only via `ensure`/`refresh`. Runtime config tests now cover managed-local start-url derivation from explicit target URL.
- Task 5 progress: route tests now prove direct-iframe health keeps CDP `not-applicable` and direct-iframe proxy responses skip the Selkies HTML shim. Full workspace typecheck remains blocked only by unrelated unused imports in `apps/desktop/src/renderer/components/nav-sidebar-tabs.tsx`.
- Task 6 progress: preload and renderer bridges now expose both `create` and `createRemote`, and browser-mode server route accepts canonical `create` payloads in addition to `create-remote`. Typecheck still fails only on unrelated `nav-sidebar-tabs.tsx` unused imports.
- Task 7a progress: browser panel form logic is now extracted into `browser-panel-form.ts`, with surface-first and runtime-ownership-aware field visibility rules. The panel still needs tests and likely cleanup for managed-local form submission and copy consistency.
- Task 7b progress: browser panel status/action copy is now extracted into `browser-panel-view-model.ts` with direct tests for direct-iframe, attached Selkies, and managed-local Selkies wording/action labels. Remaining work shifts to binding/display URL behavior in task 8.
- Task 8a progress: binding precedence helpers now live in `renderer/atoms/browser.ts` instead of ad hoc test-local logic, and released bindings explicitly clear lane affinity. Next step is wiring the actual panel to use binding-aware lane selection plus direct-iframe navigation/display semantics.
- Task 8b progress: panel lane selection now consults session binding before stored global lane via `browser-panel-selection.ts`. Direct-iframe nav/display still needs final polish and proof, but binding precedence is now wired into the real panel path.
- Task 8b progress: renamed the display helper concept to `buildBrowserPanelSurfaceUrl()` while keeping a compatibility wrapper for existing callers, and added a dedicated `getBrowserPanelNavigationStrategy()` helper so direct-iframe nav paths are explicitly URL-based rather than inferred ad hoc.
- Task 9 progress: protocol and server proxy layers now gate auth-header injection to managed-local Selkies lanes only. Added protocol-level proof that direct-iframe desktop transport forwards without injected lane auth and route-level proof that direct-iframe server proxy requests stay shim-free and auth-free.
- Task 10a progress: README and plugin-bridge docs now codify the canonical browser-lane glossary, supported combinations, and invalid combination table. Evidence files for direct-iframe, attached Selkies, and managed-local Selkies are seeded, but live screenshot/browser proof is still outstanding.
- Task 10b progress: runtime evidence now exists for managed-local profile-locked semantics and attached remote stream-without-CDP semantics. Direct-iframe runtime probe still needs a clean reachable local target; current file records the failed reachability attempt so the blocker is explicit.

- Verification pass (Opus, 2026-06-13): Ran full targeted browser-lane suite — 74 pass / 0 fail across 13 files. Server `tsgo` typecheck CLEAN; desktop `tsgo` has ZERO browser-lane errors. Fixed two real defects found during verify: (1) `browser-lane-protocol.ts` froze `REGISTRY_FILE` at module-load time, so a runtime-changed `XDG_CONFIG_HOME` was ignored and the auth-scoping logic was untestable — made registry path lazy via `getRegistryFile()`; (2) `browser-lane-protocol.test.ts` typecheck error (`Type 'never' has no call signatures`) from a `let`-from-null closure-capture pattern — replaced with a typed `CapturedProtocolHandlers` holder. Removed stray tracked TS build artifacts `apps/desktop/src/shared/browser-lanes.{js,d.ts,d.ts.map}` (stale, referenced old `mode` shape) and added a narrow `.gitignore` rule. NOT merged to main: desktop typecheck is RED only on the unrelated in-flight sidebar/storybook refactor (app-bar/nav-sidebar-* ) + 2 pre-existing streamdown/shiki errors present on `main`. Left on branch for the sidebar work to land first.
- Review-wave reconciliation (2026-06-13): fixed three real defects from F2/F3/F4 before touching plan state — attached lane proxies now preserve upstream base paths in both desktop and server routes, direct-iframe health probes no longer send managed-local auth, and direct-iframe refresh now remounts the iframe with target-specific failure copy. Focused browser-lane suite re-run after fixes is GREEN (`38 pass / 0 fail` across protocol, route, panel view-model, and browser atom tests).
- Plan-truth correction (2026-06-13): task 10 was falsely checked. It is now reopened because the current durable docs still do not satisfy the required canonical glossary / supported-combinations / invalid-combination matrix, and the direct-iframe + managed-local evidence files still explicitly admit missing live proof. Final review wave remains intentionally unchecked pending user approval per plan rules.
- Task 10c progress: durable docs are now fixed. `README.md` and `docs/palot-opencode-plugin-bridge.md` both contain the canonical glossary, supported combinations, invalid combinations, and operator examples. The remaining blocker is proof only: direct-iframe now has live route + health evidence, attached Selkies still has semantic route/health proof, but managed-local Selkies start still falls back to `profile-locked` and `GET /browser/default/` still returns `500 Internal Server Error` instead of a rendered stream.
- Task 10d progress: managed-local proof is narrower now. Starting the lane with `scripts/browser-lane/start-lane --lane default` successfully brings up the stream route (`scripts/browser-lane/healthcheck --lane default` => stream ready, `rtk curl -i http://127.0.0.1:30206/browser/default/` => `200 OK` HTML with injected shim). The route-health semantics bug is fixed too: browser-mode `/health` now reports `degraded` with `Stream route ready, CDP probe pending` instead of falling back to `profile-locked` once the stream is up.
- Remaining managed-local blocker is now runtime-only: CDP stays unreachable and container logs keep showing Selkies panics (`Io(Os { code: 95, kind: Unsupported, message: "Operation not supported" })`). That means task 10 is blocked on the Selkies runtime proving CDP, not on the browser-lane model/docs/proxy semantics anymore.
- Task 10e progress: runtime file regeneration bug is fixed too. `scripts/browser-lane/start-lane` now rewrites runtime files before `docker compose up`, which prevents stale relay scripts and stale compose port mappings from surviving helper-driven restarts. Re-ran route/runtime tests (`17 pass / 0 fail`) and revalidated the degraded managed-local proof after a full helper stop/start. Blocker remains the Selkies runtime panic / missing CDP only.
- Task 10f progress: tried two runtime mitigations and both failed to turn the blocker into a browser-lane code issue. First attempt moved the CDP relay to a distinct exposed port and proved helper regeneration really does rewrite runtime artifacts; second attempt forced `PIXELFLUX_WAYLAND=false` and `seccomp=unconfined` based on LinuxServer docs. Neither made the managed-local lane stable after a clean restart. Conclusion stands: task 10 is blocked on Selkies runtime/container behavior, not on Palot browser-lane model semantics.
