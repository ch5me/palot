## 2026-05-30T20:52:30Z Task: context-gathering <!-- oc:id=sec_aa -->
- Initial background subagent handles became unretrievable (`Task not found` / `Session not found`) after system completion notices; restarted the research lanes with fresh task IDs.
- Research caveat: plan text assumes separator drag hooks like `onDrag` / `onDragging`, but official `react-resizable-panels` docs do not expose these callbacks on `PanelResizeHandle`; implementations for Tasks 3 and 6 must adapt to real API surface.
- Task 3: `Separator` from react-resizable-panels does NOT forward `ref`, `role`, `aria-*`, or `tabIndex`. Workaround: use `elementRef` (not `ref`) for the ref, and put `role`/`aria-label`/`tabIndex` on a wrapper `<div>`. This is a limitation of the react-resizable-panels API — not configurable.
- Task 3: `data-panel-resize-handle-active="1"` is set on the Separator element while dragging (confirmed via RAF-polling). This IS the drag-detection mechanism available from the real API — no `onDrag` callback exists but this attribute does get set.
- Task 6: Mid-drag magnetic snapping is NOT possible with `react-resizable-panels`. The library provides no real-time drag position during resize. `useSnapBehavior` snaps on drag end only (via `onLayoutChange`). True mid-drag snapping would require upstream library changes or a custom drag implementation. Documented this in hook JSDoc and in learnings.
- Task 8: `ResizablePanes` initially lacked `onLayoutChange` in its props — the underlying `Group` supports it but the wrapper didn't expose it. Added `onLayoutChange: (layout: Layout) => void` to `ResizablePanesProps` and wired through to `Group`. This was needed for the honest snap story. The `Layout` type from react-resizable-panels is `{ [id: string]: number }`, not `readonly number[]` — the type annotation matters for correct usage.