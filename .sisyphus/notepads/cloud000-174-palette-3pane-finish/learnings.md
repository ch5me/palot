
- 2026-06-14 Task 1: `agent-detail.tsx` was still restoring doc lane partly from `sidePanel.activeTab`; utility and doc tabs needed separate availability reconciliation to avoid cross-lane fallback.
- 2026-06-14 Task 1: persisted `elf:firefly-surface-preferences` already had doc-specific fields in tree state, but legacy `lastSidePanelTab` values still needed migration so restore semantics stay deterministic for existing users.

- 2026-06-14: One shared `sidePanelActiveTab` made doc restore ambiguous because renderer, session switch restore, and Palot bridge events all used same tab memory for two different lanes.
- 2026-06-14: Smallest durable seam was not "derive doc open from active tab"; explicit doc-lane snapshot state was needed so unavailable doc surfaces can close lane cleanly while utility lane keeps its own memory.
- 2026-06-14 Task 2: lane membership needed renderer-free authority in `shared/firefly-surface-ids.ts`, otherwise registry rows and catalog descriptors could drift into separate doc-vs-utility truths.
- 2026-06-14 Task 2: catalog-served side-panel tabs can derive lane from projected host target slot (`main-pane` => document lane, `side-panel` => utility lane) without reintroducing id string matching in renderer routing.
- 2026-06-14 Task 2: `lane` on `FireflySidePanelTab` is smallest shared seam because both hardcoded registry rows and catalog-projected tabs already collapse into that descriptor before renderer routing happens.
- 2026-06-14 Task 3: Storybook proof needed the exact Palot nesting order from `agent-detail.tsx` — left shell split, outer utility split, then inner document split — otherwise the doc/utility coexistence claim stayed too abstract.
- 2026-06-14 Task 3: `react-reverse-portal` is still useful in Storybook even with mock data because it proves doc surfaces can stay mounted while the active document tab swaps inside one inner pane.
- 2026-06-14 Task 4: `fireflySurfaceDefaults` in `feature-flags.ts` and `defaultOn` in `firefly-surface-registry.tsx` are two sources of truth for the same concept; drift between them is invisible at build time and only shows up when a user has no persisted preference yet.
- 2026-06-14 Task 4: duplicate command-palette toggle entries for the same surface are easy to introduce when two developers add the same feature flag command in separate branches; they render as two identical menu items with no visual distinction.
- 2026-06-14 Task 5: current bridge snapshot still serializes everything under `sidePanel`, so runtime proof has to assert doc-lane intent indirectly via document-only surface ids plus unchanged mixed available-tab inventory.
- 2026-06-14 Task 5: real bridge/runtime proof can stay honest without renderer mounts by checking that `open_side_panel` accepts `studio` and `pdf-review`, rebroadcasts those exact ids, and preserves utility-surface availability instead of collapsing to a doc-only tab set.
- 2026-06-14 Task 6: browser-mode live proof showed the nested shell really composes as utility lane + document lane at once; switching utility from `Browser` to `Files` did not dislodge the document lane, and switching doc from `studio` to `pdf-review` did not dislodge the utility lane.
- 2026-06-14 Task 6: disabling an active doc surface (`pdf-review`) triggers the expected doc fallback to `studio` instead of leaving the document pane empty, so the explicit document-lane state contract is behaving on the live browser runtime.
- 2026-06-14 Task 6: browser-only `Files` surface still renders an explicit Electron-only limitation message (`Directory listing is only available in Electron mode`), which is useful honest proof that the utility lane was real even though that surface's feature depth is mode-dependent.
- 2026-06-14 Task 7: closeout note should separate "complete" work from "blocked" work explicitly; the Electron `bun:` ESM crash is a pre-existing dev-environment issue, not a regression from the 3-pane finish plan, so the ticket closeout must name it as a blocker without implying the plan caused it.
- 2026-06-14 Task 7: all 14 implementation files from Tasks 1-6 landed in the working tree as uncommitted changes; the closeout task should not commit them (that is a separate ship step), but the closeout note must reference exact file paths and diff stats so the commit step is trivially scoped.
- 2026-06-14 staging policy: when a known dev-environment blocker (like the Electron `bun:` ESM crash) prevents one runtime path from being proved, the narrowest durable fix is to put a temporary explicit skip policy in AGENTS.md rather than leaving final verification reviewers to re-discover and re-debate the same blocker on every pass.
- 2026-06-14 staging policy: out-of-scope `.sisyphus` artifacts (like `agentic-perf-lint-lsp-investigation.md`, which was a general perf investigation from a different session) cause final scope review to reject the slice as unrelated churn; removing them before re-running verification is cheaper than arguing they are harmless.
- 2026-06-14 final-review repair: main/preload/plugin snapshot seams all had to learn `documentPanel` explicitly; leaving doc state folded into `sidePanel` kept bridge/runtime proofs dishonest even after renderer lane split was correct.
- 2026-06-14 final-review repair: renderer-local tab/open changes need explicit sync back into Electron main snapshot; bridge consumers only read main-process state, so local Jotai truth alone is stale by design.
- 2026-06-14 final-review repair: utility open affordance must key off available utility-lane tabs only; counting document-only tabs makes shortcut/button state lie and can open an empty utility rail.
- 2026-06-14 final-wave repair: the shared `palotUiStateSnapshotSchema` was still using the broad side-panel tab enum for `documentPanel`; narrowing it to `DOCUMENT_SURFACE_IDS` keeps main/preload/plugin truth aligned with renderer lane ownership.
- 2026-06-14 final-wave repair: reverting unrelated Electron/runtime dependency experiments from the slice is cheaper and safer than trying to justify them during a reviewer-directed repair pass.
- 2026-06-14 final-wave repair follow-up: `palot:open-side-panel` must broadcast same payload object shape preload exposes (`{ tab }`); raw string payload slips through tests until renderer callback destructures `undefined`.
