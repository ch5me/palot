
- 2026-06-14 Task 1: doc lane stays explicit durable UI state. Contract = `documentPanelOpen` + `lastDocumentPanelTab` persist separately from `lastUtilitySidePanelTab`; utility pane keeps its own persisted tab/open state.
- 2026-06-14 Task 1: unavailable remembered doc tab falls back in `setAvailableDocumentPanelTabsAtom` to the first available doc surface; if no doc surfaces remain, close the doc lane explicitly.
- 2026-06-14 Task 1: bridge/runtime open path distinguishes doc vs utility by routing through `isDocumentPanelTab(tab)` in `agent-detail.tsx` and opening the correct pane state atom.
- 2026-06-14 Task 2: durable lane field name is `lane`, with values `"document" | "utility"`; source of truth lives in `shared/firefly-surface-ids.ts`, registry rows copy that meaning explicitly, and catalog descriptors carry same concept into `FireflySidePanelTab`.
- 2026-06-14 Task 2: catalog lane derivation uses projected host target slot (`hostTarget.slot === "main-pane"` => document lane) so future catalog-migrated doc surfaces need one metadata declaration, not new routing helper code.
- 2026-06-14 Task 2: lane membership now rides on surface descriptors as `lane: "utility" | "document"`; built-in rows declare it directly and catalog tabs project it from host target metadata instead of id special-casing.
- 2026-06-14 Task 3: Storybook proof story lives in `packages/ui/src/stories/ai-elements/palot-three-pane-shell.stories.tsx` and uses `layout: "fullscreen"` so the nested panes read like the real shell, not a card demo.
- 2026-06-14 Task 3: document lane surfaces use static mock content instead of live iframe/runtime coupling; the proof target here is nested pane behavior plus portal-backed tab switching, not backend/browser integration.
- 2026-06-14 Task 4: removed duplicate PDF Review toggle from command palette; kept the first entry (keywords: pdf, document, reader, annotations, citations) as it has slightly broader search coverage.
- 2026-06-14 Task 4: aligned `browser` surface registry `defaultOn` from `false` to `true` to match `fireflySurfaceDefaults.browserPanelEnabled: true` and the `browserPanelEnabledAtom` initial value. This was the only mismatch across all 17 registry surfaces.
- 2026-06-14 Task 5: automated bridge proof for doc surfaces lives in main-process/runtime tests, not renderer-only tests, because the acceptance seam here is `open_side_panel` over the real Palot bridge plus plugin runtime transport.
- 2026-06-14 Task 5: until the preload snapshot gets first-class doc-lane fields, the test oracle is `document` lane membership for `studio`/`pdf-review` plus preserved mixed `availableTabs` and exact `palot:open-side-panel` broadcasts for those ids.
- 2026-06-14 Task 6: browser-mode evidence for the final closeout should cite both textual DOM captures and the screenshot artifact, because the screenshot alone does not fully prove doc/utility/fallback state transitions.
- 2026-06-14 Task 6: Electron proof remains blocked until the `bun:` ESM loader crash is fixed; do not treat browser-mode proof as a substitute for the missing Electron runtime path.
- 2026-06-14 Task 7: closeout artifacts live under `.sisyphus/evidence/task-7-closeout.md` (full evidence summary) and `.sisyphus/evidence/task-7-ticket-closeout.md` (ticket-ready paste text). Notepads are appended, never overwritten.
- 2026-06-14 Task 7: the ticket closeout text explicitly separates "code complete + browser proved" from "Electron blocked" so the ticket can be updated honestly without overstating or understating the shipped state.
- 2026-06-14 staging policy: added temporary explicit policy to AGENTS.md stating browser-mode proof is sufficient for Palette/three-pane finish work until the Electron `bun:` ESM loader crash is fixed. Policy names the exact blocker, names when it expires (after staging launch), and is explicitly not permanent.
- 2026-06-14 staging policy: removed `.sisyphus/agentic-perf-lint-lsp-investigation.md` because it was clearly out of scope for CLOUD000-174 (general perf investigation from a different session) and was causing final scope review to reject the slice as unrelated churn.
- 2026-06-14 final-review repair: `PalotUiStateSnapshot` now carries two honest lanes — `sidePanel` for utility surfaces and `documentPanel` for doc surfaces — and both preload IPC plus plugin `ui_state` output must serialize both.
- 2026-06-14 final-review repair: renderer owns interactive lane state, but Electron main remains bridge source of truth, so renderer pushes a normalized snapshot through `palot:ui-state-snapshot:set` whenever utility/doc lane state changes.
- 2026-06-14 final-review repair: kept `apps/desktop/src/main/palot-plugin/plugin.js` in slice because reviewer finding hits `ui_state` bridge output directly; removed unrelated Electron experiment changes from `apps/desktop/package.json`, `apps/desktop/src/main/palot-runtime/artifact-store.ts`, and `bun.lock`.
- 2026-06-14 final-wave repair: `PalotDocumentPanelSnapshot` should expose `DocumentPanelTabId`, and the shared bridge schema should validate against `DOCUMENT_SURFACE_IDS`, not the full surface list.
- 2026-06-14 final-wave repair: utility-pane toggles stay disabled unless utility-lane tabs exist, and the handlers themselves no-op under that condition so doc-only availability cannot open an empty utility rail via keyboard/command entry points.
- 2026-06-14 final-wave repair follow-up: `palot:open-side-panel` payload contract is object form `{ tab: SidePanelTabId }` across main broadcast, preload callback typing, backend subscription wrapper, and renderer consumer tests.
