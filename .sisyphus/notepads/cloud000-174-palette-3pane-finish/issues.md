
- 2026-06-14 Task 1 follow-up for Task 2: preload/runtime snapshot shape is still named `sidePanel` even when it targets doc surfaces too. Contract is now deterministic in renderer state, but naming cleanup can wait for metadata/defaults pass.
- 2026-06-14 Task 2 follow-up for Task 4/5/6: plugin manifest/projection still has no first-class lane field; current catalog path derives lane from `hostTarget.slot`. Good enough for this seam, but true plugin-authored doc surfaces may want explicit manifest vocabulary in later contract work.
- 2026-06-14 Task 2 follow-up: catalog lane classification currently infers `document` from `hostTarget.slot === "main-pane"`; if future catalog surfaces need document-lane semantics without main-pane slot, projection schema will need first-class lane metadata.
- 2026-06-14 Task 3 caveat for Task 6: Storybook proof intentionally avoids live iframe/browser/runtime mounting, so Task 6 still needs end-to-end evidence that real `studio` and `pdf-review` surfaces survive bridge-driven opens, restore, and coexistence with an active utility lane.
- 2026-06-14 Task 4 deferred: `fireflySurfaceDefaults` and registry `defaultOn` are still two separate declarations. A future hardening pass could derive one from the other (or add a build-time assertion) to prevent re-drift. Low urgency — only 1 mismatch found across 17 surfaces.
- 2026-06-14 Task 4 deferred: no other discoverability issues found beyond the duplicate PDF Review toggle. Surfaces with `defaultOn: false` (pulse, memory, ch5pm, pdf-review) are intentionally off by default per feature-flag policy; no misleading hidden-on surfaces detected.
- 2026-06-14 Task 5 caveat for Task 6: automated runtime proof now covers bridge/plugin dispatch for `studio` and `pdf-review`, but manual proof still needs real renderer composition evidence that the inner document split opens while the outer utility split can stay closed/independent on screen.
- 2026-06-14 Task 6 blocker: Electron dev runtime crashes during app load with `ERR_UNSUPPORTED_ESM_URL_SCHEME` for protocol `bun:` before a usable window appears. Evidence: `.sisyphus/evidence/task-6-electron-pane.txt`.
- 2026-06-14 Task 6 caveat: browser lane binding was unavailable (`browser_status` returned `unbound_session`), so browser proof used real Chrome + OS automation instead of the bound lane. Evidence is still real runtime evidence, but this constraint should be called out explicitly in closeout.
- 2026-06-14 Task 7 residual: working tree has 14 modified/new files (+515/-90) from Tasks 1-6 that are not yet committed. The closeout note references them but does not commit them; a separate ship/commit step is needed before the ticket can be marked fully closed.
- 2026-06-14 Task 7 residual: type-check (`bun run check-types`) and lint (`bun run lint`) have not been re-run on the final working tree state as part of closeout. The final verification wave (F1-F4) should include these checks before commit.
- 2026-06-14 Task 7 residual: Electron `bun:` ESM loader crash is a pre-existing dev-environment blocker unrelated to this plan's changes. It needs a separate fix before Electron-mode three-pane proof can be completed.
- 2026-06-14 staging policy: temporary policy added to AGENTS.md to skip Electron-side manual proof until after staging launch. This does not weaken other quality gates; it only acknowledges that the Electron dev runtime is currently unusable due to the `bun:` ESM crash.
- 2026-06-14 staging policy: `.sisyphus/agentic-perf-lint-lsp-investigation.md` was removed because it was unrelated to CLOUD000-174 and was causing scope review to reject the slice. If future work needs that investigation, it should live in a separate notepad or issue, not in this ticket's artifact tree.
- 2026-06-14 final-review repair residual: `palot:open-side-panel` event name still speaks in old side-panel vocabulary even when opening document lane tabs; behavior is now honest via `documentPanel` snapshot, but event naming remains legacy follow-up rather than part of this focused repair.
- 2026-06-14 final-wave repair residual: `palot:open-side-panel` / `open_side_panel` naming is still legacy utility wording for a mixed-lane command. Snapshot truth is fixed, but renaming the bridge/tool surface is out of scope for this reviewer pass.
- 2026-06-14 reviewer-followup residual: legacy event/tool naming still says `side-panel` even when payload now safely opens document-lane tabs; contract coherent, vocabulary still deferred by scope.
- 2026-06-14 final-wave repair scoped cleanup: `.sisyphus/boulder.json` was unrelated planner-state churn and should stay out of this ticket slice.
- 2026-06-14 final-wave regression repair: `palot-browser-ipc` still uses module-global singleton state, so any future seam test that does not call the explicit reset helper can silently inherit prior bridge/UI state and go false-green again.
